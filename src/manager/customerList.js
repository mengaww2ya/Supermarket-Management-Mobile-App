import { SafeAreaView, View, Text } from "react-native";
import customer from "../global/data";
export default function customerList() {
  return (
    <SafeAreaView>
      <View>
        <Text>These all are registed superket customers</Text>
      </View>
    </SafeAreaView>
  );
}
